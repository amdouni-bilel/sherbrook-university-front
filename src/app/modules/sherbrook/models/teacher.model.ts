export interface TeacherModel {
    id: number;
    firstName: string;
    lastName: string;
    email: string;
    phone?: string;
    department?: string;
    createdAt: string;
    updatedAt: string;
}

